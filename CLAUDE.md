# SOL:VE 프로젝트 규칙

## 프로젝트

신한 SuperSOL 보험 Tab UX 개선. 2026-08-05 ~ 2026-09-20.
전체 계획은 00_project/PROJECT_PLAN.md 참조.

## 절대 규칙 (Research Rule)

1. AI가 발견한 UX 문제를 사용자 Pain Point로 단정하지 않는다.
   반드시 "~할 가능성이 있다" 형태로 표현한다.
2. 팀이 발견한 문제도 사용자 검증 전에는 Problem Hypothesis다.
3. 사실(Fact) / 외부 Research / 팀 Observation / AI Inference를 반드시 구분 표기한다.
4. 외부 자료는 원출처를 사용하고 발행기관·발행일·URL·페이지를 기록한다.
5. 근거 없는 내용을 사실처럼 생성하지 않는다. 모르면 "확인되지 않음"이라 쓴다.
6. 경쟁사 기능 주장은 근거 등급(A~D)을 표기한다. D등급은 카드를 만들지 않는다.
7. 화면 분석 시 [화면에서 확인되는 사실]과 [UX 해석]을 분리한다.
8. 여러 Evidence가 같은 문제를 가리키면 서로 연결해 보여준다.
9. 사용자 조사 결과와 AI 예상이 다르면 무조건 사용자 조사가 우선한다.
10. UX 개선안은 Validated Pain Point에 연결되어야 한다.
11. AI가 예상한 개선 효과는 가설일 뿐이다. 효과 판단은 실제 테스트 결과로만 한다.
12. Evidence → Hypothesis → Validation → Solution → Validation 추적성을 유지한다.

## ID 규칙

- O-### 팀 관찰 / E-### 데스크 / S-### 화면 / C-### 경쟁사 / X-### 멘토 확인
- H-### 가설 / P-### 검증된 Pain Point / SOL-### 개선안 / M-### 측정결과
- ID 대역(카드 종류 무관, 사람당 하나): 팀장 100번대 / 팀원1 200번대 /
  팀원2 300번대 / 팀원3 400번대
  → O·E·S·C·X·SOL에 적용한다. 번호는 **종류마다 따로** 센다.
    (예: 팀원3 → O-401, O-402 … / E-401, E-402 … / S-401 … / SOL-401 …)
    종류가 다르면 같은 숫자를 써도 된다. ID는 접두어까지가 이름이다.
  → H·P·M은 대역이 없다. 001부터 순번으로 발급하며 회의에서만 생성된다.
    따라서 H·P·M은 대역 위반 검사(traceability_check #9)의 대상이 아니다.
- 카드는 절대 삭제하지 않는다. status 변경만 허용한다.
  → 초안도 마찬가지다. 버릴 H 초안은 지우지 말고 `status: dropped`로 바꾼다.
    번호를 지우면 다음 실행에서 같은 번호가 다른 내용으로 재사용된다.
  → `status: dropped`인 카드는 검사에서 지적하지 않는다.

## 카드 승격 경로 (전역 규칙)

승격은 전부 회의의 산출물이다. Claude가 스스로 판단해 승격시키지 않는다.

- H 카드 초안은 02_hypothesis/draft/에 둔다.
  8/17 종합 회의에서 팀이 확정한 것만 02_hypothesis/로 승격된다.
- **P 카드는 /gate1 커맨드로만 생성한다.** 04_painpoint/에만 존재한다.
  다른 경로로 만들어진 P 카드는 규칙 위반이며 검사에서 ERROR로 잡는다.
- **M 카드는 /gate2 커맨드로만 생성한다.** 07_usertest/measurement/에만 존재한다.
  다른 경로로 만들어진 M 카드는 규칙 위반이며 검사에서 ERROR로 잡는다.
- Gate 통과 기준은 00_project/PROJECT_PLAN.md의 Gate 체크리스트가 원본이다.
  커맨드는 그것을 읽어 제시할 뿐, 별도 기준을 만들지 않는다.
- Gate를 통과하지 못하면 카드를 만들지 않고 status도 올리지 않는다.

## 카드 필수 필드

모든 카드는 YAML front-matter로 시작한다.

- 공통: id, type, author, date, summary (summary는 한 줄)
- E 카드: source_org, published, url 또는 local_file, page, grade
- C 카드: app, user_task, grade, capture (파일 경로), captured (캡처한 날짜)
  → 경쟁 앱은 발표 전에 업데이트된다. 언제 찍은 화면인지가 없으면 근거로 쓸 수 없다
- S 카드: screen (캡처 경로), captured (캡처한 날짜)
  → 앱은 업데이트되므로 언제 찍은 화면인지가 없으면 근거로 쓸 수 없다
- H 카드: status, evidence[], source_types[], validated_by
  → source_types는 서로 다른 종류가 최소 2개여야 한다
  → validated_by는 초안에서는 비워둔다. draft/ 밖으로 승격되면 반드시 채운다
- P 카드: hypothesis(H-### 필수), survey_q[], n, rate, gate
  → 하나라도 비면 카드를 만들지 않는다
- X 카드: question(질문 원문), asked(물어본 날), answer_type, confidential
  → answer_type 은 fact 또는 opinion. **opinion 은 H·SOL의 근거로 쓸 수 없다**
  → confidential: yes 는 발표 자료에 쓰지 않는다
  → 질문 하나 = 카드 한 장. 회의 한 번을 한 장에 몰아쓰지 않는다
- SOL 카드: pain_point (P-### 필수)
- M 카드: solution(SOL-### 필수), pain_point(P-### 필수), metric, baseline, result, gate

## 근거 등급 (grade)

`grade` 필드를 갖는 카드 종류 전부에 적용한다 — **E 카드와 C 카드.**

| 등급 | 무엇 | 어떻게 쓰나 |
|---|---|---|
| A | 우리가 직접 찍은 화면·직접 관찰한 것 | 근거로 쓴다 |
| B | 공식 문서·공지사항·앱스토어 설명·기관 발간 보고서 | 근거로 쓴다 |
| C | 언론 기사·앱 리뷰 | **보조 근거로만.** C만 모아 가설을 만들지 않는다 |
| D | 확인 안 됨 | **카드를 만들지 않는다** (Research Rule 6) |

- E 카드는 보통 B, 기사면 C다. A는 외부 자료에 해당하지 않는다.
- C 카드(경쟁 앱)는 우리가 직접 찍었으므로 보통 A다.
- **D 등급 카드가 존재하면 검사 #7이 WARN으로 잡는다.** 등급을 올릴 근거를
  찾거나, 못 찾으면 `_meta/source_wishlist.md`에 남긴다.

## 캡처 파일

- 캡처 이미지는 종류와 무관하게 **01_evidence/screens/ 한 곳에** 둔다.
  01_evidence/screen/ 은 S 카드가 들어가는 곳이며 이미지 폴더가 아니다.
- 파일명은 **저니 이름 방식** 하나만 쓴다 (2026-08-11 채택. 앱 종류를 가리지 않는다).

  `{앱}_{저니번호}_{화면이름}_{순번}_{YYYYMMDD}.{png|jpg}`

  예: `monimo_3-1_보험분석_04_20260810.jpg` · `supersol_2-0_보험탭_01_20260812.png`

  - **저니번호** — 찍은 사람이 앱을 돌아본 순서(`1-0`, `2-0-0`, `3-1`).
    하위 번호는 같은 화면에서 다르게 눌렀을 때 갈라진 갈래를 뜻한다.
  - **순번** — 그 화면 안에서의 순서. **두 자리로 쓴다**(01, 02).
    한 자리로 쓰면 정렬이 1, 10, 11, 2 로 꼬인다.
  - **날짜** — 넣은 날이 아니라 **찍은 날**. 재촬영본이 앞의 파일을 덮어쓰지 않게 한다.
  - **T 번호(T01~T05)를 파일명에 넣지 않는다.** 한 화면이 여러 Task에 걸치는 일이
    잦아 파일명 하나로는 담기지 않는다. 어느 캡처가 어느 T에 해당하는지는
    기록지·C 카드·`01_evidence/screens/README_{앱}_캡처.md` 대조표가 갖는다.
  - 나중에 이름을 바꾸는 단계는 없다. **처음 넣은 이름이 최종 이름이다.**
- 형식은 PNG 또는 JPG만 쓴다. **HEIC는 읽을 수 없다.**
  아이폰 기본 설정이 HEIC이므로 촬영 전에 확인이 필요하다.
- 캡처는 저장소에 파일로 있어야 한다. 채팅에 붙여넣은 이미지는
  카드의 screen·capture 필드에 적을 경로가 없어 카드를 만들 수 없다.
  → screen·capture 가 01_evidence/screens/ 밖을 가리키거나, 파일이 실제로 없거나,
    PNG·JPG가 아니면 검사(#17)가 ERROR로 잡는다.

## 멘토 확인 — 신한라이프 현업 멘토

물어볼 것은 `_meta/mentor/ask_{이름}.md`에 쌓는다. 사람마다 파일이 하나다.
쓰는 법은 `_meta/mentor/README.md`에 있다.

- **멘토 답변은 사용자 조사를 대체하지 않는다.** Research Rule 9가 우선이다.
  - 사실(제휴 구조·정책·가능 여부)을 알려준 것 → 근거로 쓴다
  - 의견("이게 낫겠다")을 준 것 → 참고만 한다. 가설·개선안의 근거가 아니다
- 판단을 묻는 질문에는 **우리 결론을 먼저 말하지 않는다.** 유도가 된다.
- 멘토 실명·직책은 기록하지 않는다. 대외비라고 한 것은 발표에 쓰지 않는다.
- 답변은 X 카드(`01_evidence/expert/`)로 남긴다. 질문과 답이 한 장에 들어간다.
  회의 전 질문 정리는 `mentor-liaison` 도우미가 한다.

## 금지 표현

다음 표현을 검증 전 단계에서 쓰지 않는다.
"사용자는 ~를 불편해한다" / "~가 문제다" / "명백히" / "확실히"
→ "~할 가능성이 있다" / "~로 보인다" / "가설: ~"

## 파일 소유권 — 폴더가 아니라 ID 대역

- 소유 단위는 폴더가 아니라 ID 대역이다. 폴더는 분류일 뿐 소유물이 아니다.
- 각자 자기 대역(위 "ID 규칙" 참조) 번호의 카드만 생성·수정한다.
  남의 대역 카드는 수정하지 않는다.
- 여러 명이 같은 폴더에 카드를 만드는 것은 정상이며 위반이 아니다.
  01_evidence/team/(전원) · 01_evidence/desk/(팀장·팀원2·팀원3) · 05_solution/(팀장·팀원2)
- 번호가 없는 공용 파일(_meta/decision_log.md, 08_deliverable/outline.md 등)은
  팀 회의 중에만 수정한다.
- **예외 — 경쟁 앱 평가지는 팀장과 팀원2가 함께 관리한다.**
  `00_project/competitor_eval_sheet.md` · `00_project/competitor_eval_guide.md`
  두 파일은 회의를 기다리지 않고 **팀장·팀원2 누구든** 항목을 고치거나 더할 수 있다.
  - 캡처를 찍는 중에 항목이 안 맞는 것이 드러나면 그 자리에서 고쳐야 하는데,
    다음 회의까지 기다리면 이미 다 찍은 뒤가 된다.
  - 고치기 전에 `/start`로 최신본을 받고, 고친 뒤에는 평가지 맨 아래
    **"고친 기록"에 한 줄** 남긴다 (날짜 · 누가 · 뭘 · 왜).
  - **항목을 지우지 않는다.** 안 쓰게 된 항목은 지우지 말고 "안 씀"이라고 표시한다.
    이미 채운 사람의 기록이 갈 곳을 잃기 때문이다.
  - 개인 기록지(`01_evidence/competitor/평가지_{앱}_{사람}.md`)는 원래부터
    각자 자기 것이다. 이 예외와 무관하며, 남의 기록지는 여전히 고치지 않는다.
  - 팀원1·팀원3은 고칠 것이 있으면 팀장에게 말한다.

## 커밋 — Claude를 기여자로 넣지 않는다

**커밋 메시지에 `Co-Authored-By: Claude ...` 트레일러를 넣지 않는다.**
어떤 경로로 커밋하든 마찬가지다 — `/save`로 하든, 사람이 직접 시키든.
`Generated with Claude Code` 같은 문구도 넣지 않는다.

- 이 저장소의 커밋 작성자는 팀원 4명뿐이다. 도구는 기여자가 아니다.
- 넣으면 GitHub 커밋과 기여자 목록에 Claude가 사람처럼 표시된다.
  학교 프로젝트 산출물이라 그렇게 보이면 안 된다.
- **AI를 어디에 썼는지는 다른 곳에 기록한다** — `_meta/decision_log.md`와
  발표 자료 "AI를 쓴 범위"(`08_deliverable/limitations.md`)가 그 자리다.
  숨기려는 것이 아니라 적을 곳이 따로 있다는 뜻이다.

커밋 메시지 형식은 `.claude/commands/save.md`가 정본이다.

## 개인정보 — 커밋 경로 화이트리스트

- 커밋해도 되는 경로의 정의처는 scripts/paths.py 하나다.
  판정 로직을 커맨드나 다른 스크립트에 복제하지 않는다.
- 커밋 전에는 python3 scripts/check_commit_paths.py 로 대조한다.
- .gitignore는 1차 방어선일 뿐이다. **이미 추적 중인 파일은 막지 못한다.**
  실제 방어는 위 경로 검사가 한다.
- 03_validation/raw/, 07_usertest/raw/ 의 내용물과 연락처·녹화 파일은
  어떤 경우에도 커밋하지 않는다. git history는 되돌릴 수 없다.
